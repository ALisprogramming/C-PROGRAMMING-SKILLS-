#include <stdio.h>
int main()
{
    int c=0;
    char str[100],*p;
    printf ("\nenter the sting:");
    fgets(str,sizeof(str),stdin);
    p=str;
    while (*p !='\0')
    {
        if(*p !='\n')
        {
       c++;
        }
    p++;
}
    printf("\nlenght=%d\n",c);
    return 0;
}